# إعداد نسخة West ERP Test على Render

هذه الحزمة مخصصة لبيئة اختبار مستقلة ولا تستخدم قاعدة بيانات النظام الرئيسي.

## الخطوات
1. أنشئ فرع GitHub جديدًا باسم `staging` أو مستودعًا منفصلًا للاختبار.
2. ارفع محتويات هذه الحزمة إلى جذر الفرع أو المستودع.
3. من Render اختر **New > Blueprint** واربط المستودع.
4. سيقرأ Render ملف `render.yaml` وينشئ خدمة `west-erp-test` وقاعدة `west-erp-test-db`.
5. بعد اكتمال النشر افتح الرابط الجديد.
6. بيانات الدخول الأولية: `admin` / `admin123`. غيّر كلمة المرور فورًا.
7. افتح **الإعدادات والإدارة > قائمة اختبار التثبيت** واتبع البنود بالترتيب.

## قواعد الأمان
- لا تربط خدمة الاختبار بقاعدة بيانات الإنتاج.
- لا ترفع بيانات حقيقية حساسة إلى بيئة الاختبار.
- ارفع التحديثات أولًا إلى staging، وبعد نجاح الاختبار ارفعها إلى production.
